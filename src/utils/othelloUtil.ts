import { CellStatus } from '../types';
import { getSample1 } from '../mock';

export const reverse = (): Array<Array<CellStatus>> | null => {
  const result = getSample1();
  return result;
};

const getPiecesNum = () => {
  return null;
};
